# Caddyfile for SecScan backend
# Listen on all interfaces so EC2 public IP changes do not require edits here.

{
    # No global options needed for internal TLS.
}

:80 {
    reverse_proxy backend:8000
}

:443 {
    tls internal
    reverse_proxy backend:8000
}
