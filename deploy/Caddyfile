# Caddyfile for SecScan backend (diagnostics)
# Minimal HTTPS proxy using Caddy's internal CA for IP testing.

{
    # No global options needed for internal TLS.
}

:443 {
    tls internal
    reverse_proxy backend:8000
}
